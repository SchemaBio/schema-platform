# ============================================
# Schema Platform 环境配置
# ============================================
#
# 使用方法:
# 1. 复制此文件为 .env
# 2. 根据需要修改配置
# 3. 运行 ./start.sh [命令]
#
# 启动命令:
#   ./start.sh               # 启动所有服务 (内置数据库)
#   ./start.sh germline      # 只启动 Germline
#   ./start.sh somatic       # 只启动 Somatic
#   ./start.sh --external-db # 使用外部数据库启动
#
# ============================================

# ============================================
# PostgreSQL 数据库配置
# ============================================

# 数据库主机 (内置数据库用 postgres，外部数据库用实际地址)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=schema_platform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres


# ============================================
# 端口配置 (外部访问)
# ============================================

# Germline 前端端口
GERMLINE_WEB_PORT=3001

# Somatic 前端端口
SOMATIC_WEB_PORT=3002


# ============================================
# 镜像配置 (可选)
# ============================================

# 使用预构建镜像而非本地构建
# 取消注释并修改为镜像地址
# GERMLINE_WEB_IMAGE=ghcr.io/username/schema-web-germline:latest
# SOMATIC_WEB_IMAGE=ghcr.io/username/schema-web-somatic:latest
# GERMLINE_BACKEND_IMAGE=ghcr.io/username/schema-backend-germline:latest
# SOMATIC_BACKEND_IMAGE=ghcr.io/username/schema-backend-somatic:latest


# ============================================
# 其他配置
# ============================================

# 时区配置
TZ=Asia/Shanghai
